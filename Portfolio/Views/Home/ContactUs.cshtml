@model DataAccessLayer.ModelLayer.ContactUsPageModel
@{
    ViewData["Title"] = "ContactUs";
    Layout = "~/Views/Shared/Home/_HomeLayout.cshtml";
}

@{
    var socialList = Model != null ? Model.SocialUrlList != null ? Model.SocialUrlList : new List<DataAccessLayer.ModelLayer.SocialProfileModel>() : new List<DataAccessLayer.ModelLayer.SocialProfileModel>();

    var overSkillLevel = Model != null ? Model.skillCalculationOverAll != null ? Model.skillCalculationOverAll : new Dictionary<string, decimal>() : new Dictionary<string, decimal>();
}

    <style>

       /*  .contact-section, .portfolio-section, .skills-section, .testimonials-section, .achievements-section {
            background-color: #f9f9f9;
            padding: 50px 20px;
        }

        .form-control {
            border-radius: 10px;
        }

        .btn-custom {
            background-color: #007bff;
            color: white;
            border-radius: 10px;
            padding: 10px 20px;
            font-size: 16px;
        }

            .btn-custom:hover {
                background-color: #0056b3;
            color: white;
            }

        .social-media-section a {
            text-decoration: none;
            font-size: 16px;
            padding: 10px 15px;
        }

        .social-media-section i {
            margin-right: 5px;
        }

        .counter {
            text-align: center;
            margin-top: 30px;
        }

        .counter-value {
            font-size: 50px;
            font-weight: bold;
            color: #007bff;
        }

        .portfolio-gallery img {
            max-width: 100%;
            border-radius: 10px;
        }

        .testimonials-card {
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

            .testimonials-card p {
                font-style: italic;
            }

        .testimonial-name {
            font-weight: bold;
        }

        .btn-outline-custom {
            border-radius: 10px;
            margin: 10px;
        }

        .progress-bar {
            background-color: #007bff;
        } */
    </style>


<div id="contact-us-page">
    <!-- Contact Section -->
    <section class="contact-section">
        <div class="container">
            <h2 class="text-center mb-1">Contact Me</h2>
            <p class="text-center mb-4">I’m always open to new opportunities and collaborations. If you have any questions, project proposals, or simply want to connect, feel free to reach out!</p>
            <h6 class="my-2 text-center"><span class="text-success">@ViewBag.EmailMessage</span></h6>
            <div class="row justify-content-between">
                <div class="col-md-7">
                    <form method="post" action="/Home/ContactUs" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="name" class="form-label">Full Name</label>
                            <input type="text" name="name" class="form-control" id="name" placeholder="Your Name">
                        </div>
                        <div class="mb-3">
                            <div class="row">
                                <div class="col-md-6">
                                    <label for="email" class="form-label">Email Address</label>
                                    <input type="email" name="email" class="form-control" id="email" placeholder="Your Email">
                                </div>
                                <div class="col-md-6">
                                    <label for="email" class="form-label">Email Address</label>
                                    <input type="number" name="mobileno" class="form-control" id="mobile" placeholder="Your Mobile No">
                                </div>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="subject" class="form-label">Subject</label>
                            <input type="text" name="subject" class="form-control" id="subject" placeholder="Subject">
                        </div>
                        <div class="mb-3">
                            <label for="message" class="form-label">Message</label>
                            <textarea class="form-control" name="message" id="message" rows="3" placeholder="Your Message"></textarea>
                        </div>
                        <div class="text-center">
                            <button type="submit" class="btn btn-custom">Send Message</button>
                        </div>
                    </form>
                </div>
                <div class="col-md-4 mt-4 mt-md-0">
                    @if (socialList.Count > 0)
                    {
                        <div class="social-media-section text-center">
                            <h4>Connect with Me</h4>
                            <div class="d-flex justify-content-center">
                                @foreach(var item in socialList){
                                    <a href="@item.UrlLink" target="_blank" class="btn mx-2 btn-outline-custom">
                                        <img height="30" src="~/images/socialicon/@item.SocialProfileIconPath" /> @item.UrlText
                                    </a>
                                }
                               @*  
                                <a href="https://github.com/yourprofile" target="_blank" class="btn btn-outline-dark mx-2 btn-outline-custom">
                                    <i class="bi bi-github"></i> GitHub
                                </a>
                                <a href="https://twitter.com/yourprofile" target="_blank" class="btn btn-outline-info mx-2 btn-outline-custom">
                                    <i class="bi bi-twitter"></i> Twitter
                                </a>
                                <a href="https://www.instagram.com/yourprofile" target="_blank" class="btn btn-outline-danger mx-2 btn-outline-custom">
                                    <i class="bi bi-instagram"></i> Instagram
                                </a> *@
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section class="skills-section">
        <div class="container">
            <h2 class="text-center mb-4">My Skills</h2>
            <div class="row">
                <div class="col-md-6">
                    <h5>Frontend Development</h5>
                    <div class="progress mb-3">
                        @if (overSkillLevel.ContainsKey("frontend"))
                        {
                            <div class="progress-bar" style="width: @overSkillLevel["frontend"]%;" role="progressbar">@overSkillLevel["frontend"]%</div>
                        }
                        else{
                            <div class="progress-bar" style="width: @overSkillLevel["frontend"]%;" role="progressbar">@overSkillLevel["frontend"]%</div>
                        }
                        
                    </div>
                </div>
                <div class="col-md-6">
                    <h5>Backend Development</h5>
                    <div class="progress mb-3">
                        @if (overSkillLevel.ContainsKey("backend"))
                        {
                            <div class="progress-bar" style="width: @overSkillLevel["backend"]%;" role="progressbar">@overSkillLevel["backend"]%</div>
                        }
                        else
                        {
                            <div class="progress-bar" style="width: @overSkillLevel["backend"]%;" role="progressbar">@overSkillLevel["backend"]%</div>
                        }
                        
                    </div>
                </div>
                <div class="col-md-6">
                    <h5>Database Management</h5>
                    <div class="progress mb-3">
                        @if (overSkillLevel.ContainsKey("database"))
                        {
                            <div class="progress-bar" style="width: @overSkillLevel["database"]%;" role="progressbar">@overSkillLevel["database"]%</div>
                        }
                        else
                        {
                            <div class="progress-bar" style="width: @overSkillLevel["database"]%;" role="progressbar">@overSkillLevel["database"]%</div>
                        }
                        
                    </div>
                </div>
                <div class="col-md-6">
                    <h5>Version Control</h5>
                    <div class="progress mb-3">
                        @if (overSkillLevel.ContainsKey("others"))
                        {
                            <div class="progress-bar" style="width: @overSkillLevel["others"]%;" role="progressbar">@overSkillLevel["others"]%</div>
                        }
                        else
                        {
                            <div class="progress-bar" style="width: @overSkillLevel["others"]%;" role="progressbar">@overSkillLevel["others"]%</div>
                        }
                        
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Portfolio Section -->
    <section class="portfolio-section">
        <div class="container">
            <h2 class="text-center mb-4">My Projects</h2>
            <div id="portfolioCarousel" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img src="project1.jpg" class="d-block w-100" alt="Project 1">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Project 1</h5>
                            <p>Project description goes here.</p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="project2.jpg" class="d-block w-100" alt="Project 2">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Project 2</h5>
                            <p>Project description goes here.</p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="project3.jpg" class="d-block w-100" alt="Project 3">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Project 3</h5>
                            <p>Project description goes here.</p>
                        </div>
                    </div>
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#portfolioCarousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#portfolioCarousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>
        </div>
    </section>

    <!-- Testimonials Section -->
    <section class="testimonials-section">
        <div class="container">
            <h2 class="text-center mb-4">What People Say</h2>
            <div class="row">
                <div class="col-md-6">
                    <div class="testimonials-card">
                        <p>"Jayanendra is an excellent developer. I worked with him on a major project and was amazed by his skills and dedication."</p>
                        <h6 class="testimonial-name">John Doe</h6>
                        <p>Project Manager</p>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="testimonials-card">
                        <p>"The quality of work is exceptional. Jayanendra's approach to problem-solving is unique and efficient."</p>
                        <h6 class="testimonial-name">Jane Smith</h6>
                        <p>Lead Developer</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Achievements Section -->
    <section class="achievements-section">
        <div class="container">
            <h2 class="text-center mb-4">My Achievements</h2>
            <div class="row">
                <div class="col-md-4">
                    <div class="counter">
                        <h3 id="projectsCompleted" class="counter-value" data-target="50">0</h3>
                        <p>Projects Completed</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="counter">
                        <h3 id="yearsExperience" class="counter-value" data-target="5">0</h3>
                        <p>Years of Experience</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="counter">
                        <h3 id="clientsWorked" class="counter-value" data-target="20">0</h3>
                        <p>Clients Worked With</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Download Resume Button -->
    <section class="container py-5 text-center">
        <h2 class="mb-4">Download My Resume</h2>
        <a href="resume.pdf" class="btn btn-custom" download>
            <i class="bi bi-file-earmark-pdf"></i> Download PDF
        </a>
    </section>
</div>

    <script>
        // Animated Counter Logic
        document.addEventListener("DOMContentLoaded", function() {
            const counters = document.querySelectorAll('.counter-value');
            counters.forEach(counter => {
                counter.innerText = '0';
                const target = +counter.getAttribute('data-target');
                const updateCounter = () => {
                    const count = +counter.innerText;
                    const increment = target / 100;
                    if (count < target) {
                        counter.innerText = Math.ceil(count + increment);
                        setTimeout(updateCounter, 10);
                    } else {
                        counter.innerText = target;
                    }
                };
                updateCounter();
            });
        });
    </script>


